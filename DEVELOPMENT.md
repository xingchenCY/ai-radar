# 开发说明

## 实际投入时间

本次实现实际在单个工作日内完成，包含来源探测、采集器、前端、测试和 Pages 工作流：

```text
需求和来源验证：45 分钟
采集器和数据合并：110 分钟
前端页面和筛选：150 分钟
自动化部署：55 分钟
测试、修复和文档：120 分钟
```

## 关键决策

1. 使用“当前已部署页面中的 snapshot.json”作为运行状态，而不是把每日数据提交到 Git、数据分支、SQLite 或 GitHub Release。这样代码仓库保持干净，构建失败时旧页面仍然可用，下次任务可以从线上旧快照继续增量合并。
2. 只做确定性的 URL、guid 和同来源标题指纹去重，不做跨媒体语义聚合。题目要求固定输入重复导入幂等，不要求判断不同报道是否是同一事件；在短时间内优先保证可解释和可验证。

## 主要开源技术

Astro、feedparser、python-dateutil、pytest、GitHub Actions、GitHub Pages。

## 问题定位记录

真实定位过程：第一次安全测试发现 `HTMLParser` 会把 `<script>` 内的文本节点输出到摘要。定位后增加 script/style/noscript/template 忽略深度，并用 XSS fixture 验证摘要只通过文本节点渲染；同时保留 malformed HTML 的可见文本和空摘要兜底。

## 主动不做的功能

不实现账号、评论、自动翻译、AI 摘要、全文抓取、跨源事件聚合、长期归档和代理池。这些功能不属于基础验收，且会扩大版权、准确性和时间风险。
